# AGENTS.md

本文件只定义仓库中的通用编码与协作规则。项目介绍、技术栈、目录结构、运行命令和架构约束统一维护在 `README.md` 及其链接的项目文档中。

## 1. 编码前先确认

- 明确需求、范围、假设和可验证的完成标准。
- 存在会显著影响结果的歧义时，先说明不同理解并询问，不要静默猜测。
- 有更简单的实现时优先采用，并说明必要的取舍。

## 2. 保持简单

- 只实现明确要求的功能，不添加推测性的特性、配置或抽象。
- 单次使用的逻辑保持直接，避免为了“以后可能需要”而过度设计。
- 优先选择改动少、容易理解和容易验证的方案。

## 3. 精准修改

- 只修改完成当前任务所必需的文件和代码。
- 保留用户已有改动，不整理、重构或格式化无关内容。
- 遵循现有代码风格；只清理由本次修改产生的无用代码。
- 发现无关问题时可以说明，但未经要求不要顺手修复。

## 4. 验证结果

- 根据改动风险选择最小且有效的检查，包括构建、类型检查、测试或静态检查。
- 修复缺陷时先确认问题可复现，再验证目标行为已恢复。
- 在宣称完成前检查实际输出和差异，不把未运行的检查描述为已通过。

## 5. 安全与版本控制

- 不提交密钥、令牌、个人数据或其他敏感信息，也不要把它们写入日志。
- 未经明确授权，不执行破坏性 Git 操作，不覆盖或丢弃用户改动。
- 提交内容应聚焦当前任务；是否提交、推送或合并由用户明确决定。
